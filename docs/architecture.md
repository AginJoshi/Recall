# Architecture

Main layers:
- `ui`: camera screen, overlay, transcript panel, saved profiles
- `cv`: face detection, face crop, face matching
- `nlp`: speech-to-text and fact extraction
- `storage`: local persistence
- `utils`: shared constants and helpers
