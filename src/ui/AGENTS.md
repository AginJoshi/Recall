# UI Agent Rules

- keep the camera view as the primary interaction surface
- show only the minimum necessary recall cues
- avoid cluttered layouts or long text blocks
- preserve readable error and unsupported-browser states
- do not move CV, NLP, or storage logic into UI modules

Preferred checks:

- verify the main camera view still renders
- verify memory cues remain readable on desktop and mobile
